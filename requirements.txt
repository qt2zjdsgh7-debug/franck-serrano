# iCloud Download (script existant)
pyicloud>=1.0.0
click>=8.0.0
tqdm>=4.60.0

# Agent WeChat
anthropic>=0.49.0
rich>=13.0.0
python-dotenv>=1.0.0

# Connexion WeChat Web (optionnel — nécessite un compte compatible)
# itchat>=1.3.10
