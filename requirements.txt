pyicloud>=1.0.0
click>=8.0.0
tqdm>=4.60.0

# Memory system (no API key needed — uses local Ollama)
ollama>=0.3.0

# Media memory (no API key needed — local embeddings + vector DB)
sentence-transformers>=3.0.0
chromadb>=0.5.0
duckduckgo-search>=6.0.0
pypdf>=4.0.0
